# =============================================================================
# CLI Proxy Configuration
# =============================================================================

# API Keys (comma-separated, leave empty for dev mode without auth)
# WARNING: Running without API keys is insecure for production use
API_KEYS=sk-local-key1,sk-local-key2

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Logging Level (debug, info, warning, error)
LOG_LEVEL=info

# =============================================================================
# Codex CLI Configuration
# =============================================================================

# Path to codex binary
# Leave empty to auto-detect, or specify custom path:
#   macOS (Homebrew): /opt/homebrew/bin/codex or /usr/local/bin/codex
#   Linux: /usr/local/bin/codex or /usr/bin/codex or ~/.local/bin/codex
#   Windows: C:\Program Files\Codex\codex.exe or just 'codex' if in PATH
CODEX_PATH=

# Codex model to use
CODEX_MODEL=o3

# Codex Sandbox Mode (read-only, workspace-write, danger-full-access)
CODEX_SANDBOX=read-only

# Enable full auto mode (true/false)
CODEX_FULL_AUTO=false

# =============================================================================
# OpenCode CLI Configuration
# =============================================================================

# Path to opencode binary
# Leave empty to auto-detect, or specify custom path:
#   macOS (Homebrew): /opt/homebrew/bin/opencode or /usr/local/bin/opencode
#   Linux: /usr/local/bin/opencode or /usr/bin/opencode or ~/.local/bin/opencode
#   Windows: C:\Program Files\OpenCode\opencode.exe or just 'opencode' if in PATH
OPENCODE_PATH=/opt/homebrew/bin/opencode
OPENCODE_MODEL=opencode/grok-code

# OpenCode model to use (format: provider/model)
# Examples: anthropic/claude-sonnet-4, openai/gpt-4o, anthropic/claude-3-5-sonnet
OPENCODE_MODEL=anthropic/claude-sonnet-4
